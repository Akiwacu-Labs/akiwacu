{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 0, "y": 0},
      "targets": [{"expr": "akiwacu_membres_total", "legendFormat": "{{tontine}}", "refId": "A"}],
      "title": "Membres actifs",
      "type": "stat"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "currencyBIF"}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 8, "y": 0},
      "targets": [{"expr": "akiwacu_cotisations_montant_total", "legendFormat": "{{tontine}}", "refId": "A"}],
      "title": "Cotisations cumulées",
      "type": "stat"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "currencyBIF"}, "overrides": []},
      "gridPos": {"h": 8, "w": 8, "x": 16, "y": 0},
      "targets": [{"expr": "akiwacu_solde_caisse", "legendFormat": "{{tontine}}", "refId": "A"}],
      "title": "Solde de caisse",
      "type": "stat"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "currencyBIF"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "targets": [{"expr": "akiwacu_prets_montant_total", "legendFormat": "{{tontine}}", "refId": "A"}],
      "title": "Encours des prêts",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "${DS_PROMETHEUS}"},
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8},
      "targets": [{"expr": "sum by (motif) (akiwacu_prets_refuses_total)", "legendFormat": "{{motif}}", "refId": "A"}],
      "title": "Prêts refusés par règle",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["akiwacu", "metier"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "Akiwacu - Métier",
  "uid": "akiwacu-metier",
  "version": 1
}

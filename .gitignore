# Java / Maven
target/
*.class
*.jar
!**/.mvn/wrapper/maven-wrapper.jar
.mvn/timing.properties

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# Node / React
node_modules/
dist/
.vite/
*.tsbuildinfo

# ─────────────────────────────────────────────────────────────
# SECRETS — JAMAIS commiter (le dépôt est PUBLIC)
# ─────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
id_ed25519*
id_rsa*
application-local.yml
application-secret.yml

# Infrastructure — valeurs sensibles
infra/terraform/terraform.tfvars
infra/terraform/*.tfstate
infra/terraform/*.tfstate.backup
infra/terraform/.terraform/
infra/ansible/group_vars/**/vault.yml
infra/ansible/*.vault_pass

# ─────────────────────────────────────────────────────────────
# CONFIGURATION LOCALE À CHAQUE MACHINE
# (personnelle, pas partagée — mais .claude/commands/ EST versionné,
#  c'est ce qui garantit que les 5 agents suivent les mêmes conventions)
# ─────────────────────────────────────────────────────────────
.claude/settings.local.json
.claude/*.local.*

# Rapports générés
/logs/
*.log
.sonar/
.scannerwork/
